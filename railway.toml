[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.install]
cmds = [
  "python -m pip install -r requirements.txt",
  "python -m playwright install --with-deps chromium"
]

[start]
cmd = "python reserve_watch.py"
